env: test
in_memory_storage: false
http_server:
  port: ":8080"
# postgres/redis values are placeholders — main.go overrides them when env=test
postgres:
  host: "-"
  port: 5432
  user: "-"
  password: "-"
  dbname: "-"
redis:
  addr: "-:-"
  password: ""
  db: 0
